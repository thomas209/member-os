// Movido a components/pos/PrintButton.tsx (se comparte con la vista publica
// del comprobante). Este archivo queda como re-export por si algo lo importa
// desde la ruta vieja.
export { default } from "@/components/pos/PrintButton";
